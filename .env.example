# Base URL of SPT-BACKEND (no trailing slash). Used by Next.js API routes (server).
# e.g. http://localhost:5000
BACKEND_URL=http://localhost:5000

# Public backend URL for real-time Socket.io (browser).
# IMPORTANT: On Vercel, the admin app cannot host Socket.IO itself (no long-lived WebSocket server),
# so you must deploy `SPT-BACKEND` separately and set this to that URL.
# Examples:
# - http://localhost:5000
# - https://your-backend-domain.com
NEXT_PUBLIC_BACKEND_URL=http://localhost:5000

# Admin login uses the credentials from SPT-BACKEND .env:
# ADMIN_EMAIL and ADMIN_PASSWORD. Create that admin once in SPT-BACKEND with:
#   npm run create-admin
